// pub struct MailEngineImpl — implemented in issue #5+
//
// Submodules contain trait implementations, one per file.

mod accounts;
mod calendar;
mod compose;
mod contacts;
mod search;
mod send;
mod store;
